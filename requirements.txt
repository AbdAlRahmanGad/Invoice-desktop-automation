# Python 3.10-3.12 only: paddlepaddle 2.6.x publishes no wheels for 3.13+.
#
#   uv venv --python 3.11 .venv && source .venv/bin/activate
#   uv pip install -r requirements.txt
#
# --- Direct dependencies (9) -------------------------------------------
# paddleocr    text detection + recognition. PINNED <3: paddleocr 3.x pulls
#              paddlex/PP-OCRv6 and crashes here with NotImplementedError
#              (ConvertPirAttribute2RuntimeAttribute) -- verified, do not bump
#              without re-testing.
# paddlepaddle inference runtime for paddleocr. PINNED to the last 2.x release
#              (2.6.2): paddle 3.x's CPU graph-fusion passes miscompile the
#              paddle-2.x-era PP-OCRv3/v4 models paddleocr 2.10 ships -- the
#              fused_conv2d they build leaves oneDNN without its Filter input
#              ("OneDnnContext does not have the input Filter"). On 2.6.2 the
#              fusion is correct and ir_optim can stay on, so ocr.py needs no
#              predictor monkeypatch. Do not bump without re-testing.
# pypdfium2    renders PDF pages to images.
# pillow       loads/upscales image inputs.
# numpy        image arrays.
# pydantic     the SalesOrder/LineItem schema.
# pywinauto    drives Fakturama through Microsoft UI Automation.
# pywin32      the Win32 calls pywinauto does not wrap: posting clicks and
#              keys to a control's own window, and PrintWindow captures of a
#              window that another window is covering.
# setuptools   NOT a build tool here: paddlepaddle imports it at runtime
#              (paddle.utils.cpp_extension) and fails on import without it.
numpy==2.4.6
paddleocr==2.10.0
paddlepaddle==2.6.2
pillow==12.3.0
pydantic==2.13.5
pypdfium2==5.13.0
pywinauto==0.6.9
pywin32==312
setuptools==84.0.0

# --- Transitive, pinned for reproducibility (46) ---------------------
albucore==0.0.24
albumentations==2.0.8
annotated-types==0.8.0
anyio==4.14.2
astor==0.8.1
beautifulsoup4==4.15.0
certifi==2026.7.22
charset-normalizer==3.5.1
colorama==0.4.6
comtypes==1.4.16
cython==3.3.0
decorator==5.3.1
fire==0.7.1
fonttools==4.64.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
imageio==2.37.4
lazy-loader==0.5
lmdb==2.3.0
lxml==6.1.3
networkx==3.6.1
opencv-contrib-python==5.0.0.93
opencv-python-headless==5.0.0.93
opencv-python==5.0.0.93
opt-einsum==3.3.0
packaging==26.3
protobuf==3.20.2
pyclipper==1.4.0
pydantic-core==2.46.5
python-docx==1.2.0
pyyaml==6.0.3
rapidfuzz==3.14.6
requests==2.34.2
scikit-image==0.26.0
scipy==1.17.1
shapely==2.1.2
simsimd==6.5.16
soupsieve==2.9.2
stringzilla==5.1.2
termcolor==3.3.0
tifffile==2026.3.3
tqdm==4.70.0
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
